# Helm status

The production Helm chart is Milestone 5 work. Docker Compose is the only supported deployment artifact in the current vertical slice. This file prevents an empty chart from being mistaken for production Kubernetes support.
