apiVersion: 1

providers:
  - name: StormRelay
    orgId: 1
    folder: StormRelay
    folderUid: stormrelay
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
