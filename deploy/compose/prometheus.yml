global:
  scrape_interval: 10s
  evaluation_interval: 30s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: stormrelay-server
    static_configs:
      - targets: ["server:8080"]
  - job_name: stormrelay-worker
    static_configs:
      - targets: ["worker:8081"]
